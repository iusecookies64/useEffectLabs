import { useState, useEffect } from "react";
import "./App.css";

function App() {
    const [users, setUsers] = useState([]);

    // use useEffect to fetch user Data and then render it on the page

    return <div className="app">{/* Render data here */}</div>;
}

export default App;
